[project]
name = "jobs-extractor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10.13"
dependencies = [
    "crawl4ai>=0.8.0",
    "markdownify>=1.2.2",
]
